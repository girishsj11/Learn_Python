[pytest]
minversion = 6.0
testpaths =
    tests
addopts = -rA -qsv --html=reports/report.html
log_cli = true
log_level = DEBUG
log_format = %(asctime)s %(levelname)s %(message)s
log_date_format = %Y-%m-%d %H:%M:%S
log_file = reports/pytest-logs.txt
